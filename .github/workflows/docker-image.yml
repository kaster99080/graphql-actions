name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-dept: 0

    - name: Git Semantic Version
      uses: PaulHatch/semantic-version@v4.0.3
      with:
        major_pattern: "major:"
        minor_pattern: "feat:"
        format: "${major}.${minor}.${patch}-prerelease${increment}"
      id: version

    - name: Docker login
      env: 
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        NEW_VERSION: ${{ steps.version.outputs.format }}
        INCREMENT: ${{ steps.version.outputs.increment }}
        VERSION_TYPE: ${{ steps.version.outputs.version_type }}
      run: |
        echo "Inciando login docker"
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
        echo "***VERSION_TYPE: -$VERSION_TYPE!!!!!"
        echo "***INCREMENT:    -$INCREMENT!!!!!"
        echo "***NEW_VERSION:  -$NEW_VERSION!!!!!""
        echo "Finalizando login docker"
        

    # - name: Build docker image
      # run: |
        # docker build -t kaster99080/graphql-actions:0.0.2 .
        # docker build -t kaster99080/graphql-actions:latest .

    # - name: Push docker image
      # run: |
        # docker push kaster99080/graphql-actions:0.0.2
        # docker push kaster99080/graphql-actions:latest
        
